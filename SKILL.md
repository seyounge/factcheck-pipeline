---
name: factcheck-pipeline
description: >
  다중 AI 모델(Codex·Gemini/Antigravity·Grok 등 설치된 CLI)로 리서치/조사 문서를 교차 팩트체크하고,
  사용자가 고른 최종 판정 모델이 결과를 종합해 최종 팩트체크 문서를 만드는 파이프라인.
  사용자가 "팩트체크", "fact check", "검증해줘", "교차 검증", "이 문서 사실 확인", "출처 확인",
  "크로스체크", "3중 체크" 등을 언급하거나, 리서치·조사 문서의 정확성을 확인하고 싶어하는 맥락이면
  — 명시적으로 '파이프라인'이라는 단어를 쓰지 않아도 — 반드시 이 스킬을 사용할 것.
  새 리서치 문서를 작성 완료한 직후 검증을 제안할 때도 이 스킬을 사용한다.
---

# 팩트체크 파이프라인

여러 AI 모델이 서로를 검증하게 하는 문서 팩트체크 파이프라인이다. 단일 모델의 셀프 체크는 자기 오류를 놓치기 쉽다 — 서로 다른 기반의 모델(예: OpenAI Codex, Google Gemini, xAI Grok)이 독립적으로 웹 검증을 수행하면, 한 모델이 놓친 서지 오류·수치 오귀속·구버전 규칙 인용을 다른 모델이 잡아낼 확률이 크게 오른다.

전체 흐름:

```
① CLI 탐지 → ② 사용자 선택(검증 도구 + 최종 판정 모델 + 검증 깊이 1·2·3회)
→ ③ 병렬 팩트체크(백그라운드) → ④ 최종 판정 모델의 교차 종합
→ ⑤ 최종 팩트체크 문서(_팩트체크_최종.md) 생성 → ⑥ (선택) 정정 반영본 _vN 포킹
→ (깊이 2·3회) 정정본을 다시 팩트체크하는 라운드 반복 — 신규 지적 0건이면 조기 종료
```

## 1단계 — 설치된 AI CLI 탐지

`scripts/detect_clis.sh`를 실행해 사용 가능한 도구와 각 도구의 최신 모델을 파악한다:

```bash
bash <skill-dir>/scripts/detect_clis.sh
```

스크립트가 없거나 실패하면 수동으로 확인한다: `which codex grok agy gemini`, 모델은 `~/.codex/config.toml`(codex) / `agy models`(Antigravity=Gemini) / `grok models`(Grok). **모델명은 수시로 바뀌므로 매번 실제 목록을 확인**하고, 팩트체크에는 속도(플래시) 계열보다 추론(Pro/high-effort) 계열을 고른다.

주의: 구 `gemini` CLI는 개인 계정 지원이 종료된 경우가 많다(IneligibleTierError) — `agy`(Antigravity CLI)가 있으면 그쪽을 쓴다.

## 2단계 — 사용자 선택 (AskUserQuestion)

탐지 결과를 바탕으로 두 가지를 묻는다:

1. **검증 도구 선택** (multiSelect): 탐지된 CLI만 선택지로 제시. 각 선택지에 사용할 모델명 명시 (예: "Codex (gpt-6-astra)"). 설치 안 된 도구는 선택지에 넣지 않는다.
2. **최종 판정(교차 종합) 모델**: 검증 결과들을 비판적으로 해석해 반영/기각을 결정할 모델.
   - 기본 권장: **Claude (현재 세션)** — 문서 맥락을 이미 알고 있고 원 출처 재확인 도구(WebFetch)를 갖고 있음
   - 대안: Codex 등 선택된 CLI 중 하나 — 이 경우 해당 CLI에 종합 판정 프롬프트를 별도 실행
3. **검증 깊이 (반복 회수)**:
   - **1회 (기본·빠름)**: 단일 패스 — 검증 → 종합 → 최종 보고서
   - **2회 (권장·꼼꼼)**: 1회 수행 후 정정 반영본을 만들고, **그 정정본을 다시 팩트체크** — 잔존 오류와 정정 과정에서 새로 생긴 오류를 잡는다
   - **3회 (가장 deep)**: 최대 3라운드 "검증 → 정정 → 재검증" 루프 — 라운드마다 검증 각도를 변주해 다른 결로 재검증

대상 문서가 지정되지 않았으면 파일 경로도 함께 확인한다.

## 3단계 — 병렬 팩트체크 실행 (백그라운드)

선택된 각 CLI를 **백그라운드로 병렬 실행**한다 (한 도구가 수 분 걸리므로 직렬 실행하지 않는다). 결과 파일 명명: `<파일명>_팩트체크_<도구>.md` (예: `..._팩트체크_codex.md`, `..._팩트체크_gemini.md`, `..._팩트체크_grok.md`).

공통 프롬프트 골격 (한국어 기준, 문서 언어에 맞춰 조정):

> "다음 문서의 사실 주장·수치·출처를 웹 검색으로 팩트체크하라: <절대경로> — 각 주장에 대해 [확인/의심/오류] 판정, 근거 URL, 정정 제안을 항목별 마크다운 표로 정리하라. 특히 논문 서지(저자·연도·저널), 제품/제도의 현행 규칙, 성과 수치의 출처 귀속을 중점 검증하라. 파일을 수정하지 말고 검증 결과만 출력하라."

도구별 실행 명령과 함정 회피는 `references/cli-commands.md`를 읽고 그대로 따른다 (셸 `$` 확장, git 체크, 웹검색 플래그 등 실제 겪은 함정들이 정리돼 있다).

실행 원칙:
- stderr를 `/dev/null`로 버리지 말 것 — `2>/tmp/<cli>_err.log`로 받아야 실패 원인(쿼터 소진·인증 만료)을 진단할 수 있다
- 실패 시 에러가 quota/rate limit/billing/인증 계열이면 **즉시 사용자에게 알리고** 남은 도구로 계속할지 묻는다. 조용히 빼고 진행하지 않는다
- `*_팩트체크*.md`와 `_v2` 이상 포킹본은 재팩트체크 대상에서 제외한다 (무한 루프 방지)

## 4단계 — 교차 종합 (최종 판정 모델)

모든 병렬 검증이 완료되면 최종 판정 모델이 결과를 종합한다. **어떤 검증 도구의 지적도 그대로 반영하지 않는다** — 팩트체커도 틀린다.

교차 종합 원칙:
1. **2개 이상 모델이 일치 지적한 항목** → 우선 반영 후보
2. **단독 지적** → 최종 판정 모델이 1차 출처를 직접 재확인(WebFetch 등) 후 판정. 중요하거나 불확실하면 다른 상위 신뢰 모델에 해당 항목만 표적 재검증
3. **모델 간 상충** → 더 구체적인 1차 출처를 제시한 쪽을 재확인해 판정. **최종적으로는 항상 1차 출처가 이긴다**
4. 접근 실패("열람 못 했다")는 반박 근거가 아니다 — 다른 모델이 확인했으면 유지하되 각주로 기록

최종 판정 모델이 Claude가 아닌 CLI라면: 검증 결과 파일들의 경로를 모두 전달하며 "각 지적을 교차 대조하고 원 출처를 재확인해 항목별 최종 판정(반영/기각)과 사유를 표로 작성하라"는 프롬프트로 실행한 뒤, Claude가 그 출력을 검토해 명백한 문제만 바로잡는다.

## 5단계 — 최종 팩트체크 문서 생성

`<파일명>_팩트체크_최종.md`를 다음 구조로 작성한다:

```markdown
# <문서명> 팩트체크 최종 보고서
- 검증일 / 대상 문서 / 사용 도구·모델(버전 명시) / 최종 판정 모델
## 종합 판정
(오류 N건·의심 N건·확인 N건 — 문서 신뢰도 한 줄 평가)
## 항목별 최종 판정표
| # | 지적 항목 | 지적 모델 | 타 모델 판정 | 최종 판정 | 재검증 방법 | 사유 |
## 우선 정정 목록
(오류로 확정된 항목의 정정문 제안)
## 검증의 한계
(접근 실패한 출처, 미검증 영역)
```

"지적 모델" 컬럼은 필수다 — 어느 모델이 무엇을 찾았는지 추적돼야 다음 검증에서 모델 신뢰도를 조정할 수 있다.

## 5.5단계 — 검증 깊이에 따른 반복 라운드 (깊이 2·3회 선택 시)

반복의 목적은 두 가지다: ①1라운드가 놓친 **잔존 오류** ②정정 과정에서 **새로 도입된 오류**(정정문 자체의 오기, 과도한 완화 등). 실전에서 1라운드 정정본을 독립 모델이 재검증했을 때 추가 지적이 나온 사례가 실제로 있다.

각 라운드 진행:

1. 직전 라운드의 최종 판정을 반영한 **정정 반영본(_v2, _v3…)을 생성**한다 (6단계 절차)
2. 그 정정본을 대상으로 **다시 병렬 팩트체크**를 실행한다 — 결과 파일은 라운드 표기: `<파일명>_v2_팩트체크_<도구>.md`
3. **라운드별 검증 각도 변주** (같은 질문을 반복하면 같은 답이 나온다 — 각도를 바꿔야 새 오류가 보인다):
   - 1라운드: 전반 검증 (사실 주장·수치·출처 전체)
   - 2라운드: **서지·수치 정밀 대조** 집중 — 저자·연도·저널·페이지, 수치의 원문 표 대조, 통계값 해석(유의성·CI)
   - 3라운드: **최신성·현행성** 집중 — 인용 규정/제품 규칙이 현행 버전인지, 기관·제도 변경 여부, 링크 유효성
4. 교차 종합 후 **신규 지적 수를 기록**하고:
   - **신규 지적 0건 → 조기 종료** (남은 라운드 생략 — "수렴 도달"로 보고)
   - 지적이 있으면 다음 라운드 반복 (선택한 깊이까지)
5. 최종 보고서에 **라운드별 수렴 표**를 포함한다:

```markdown
## 라운드별 수렴
| 라운드 | 대상 버전 | 신규 지적 | 반영 | 비고 |
| 1 | 원본 | 12건 | 10건 | |
| 2 | _v2 | 3건 | 2건 | 서지·수치 집중 |
| 3 | _v3 | 0건 | — | 수렴 도달 ✅ |
```

⚠ 루프 방지 규칙과의 관계: "`_v2` 이상 포킹본은 재팩트체크 제외"는 **의도치 않은 재귀**를 막는 규칙이다. 깊이 옵션에 따른 라운드 재검증은 **사용자가 명시적으로 선택한 의도적 재검증**이므로 예외로 진행한다 (단, `*_팩트체크*.md` 파일 자체는 어떤 경우에도 검증 대상이 아니다).

## 6단계 — (선택) 정정 반영본 포킹

사용자가 원하면 최종 판정표의 반영 확정분을 적용한 수정본을 만든다. **원본을 덮어쓰지 않고** `<파일명>_v2.md`(기존 버전이 있으면 _v3, _v4…)로 포킹하고, 문서 상단에 검증 이력 1줄, 말미에 최종 판정표를 포함한다. 원본·개별 검증 결과·최종 보고서는 모두 보존한다 (감사 추적).

## 실전에서 검증된 효과 (참고)

이 파이프라인의 원형은 실제 의료 리서치 문서에서 다음을 잡아냈다: 논문 제1저자 오기, 연구 성과의 상업제품 오귀속, 제품 규칙의 구버전 인용, 정부 사업 시행일 오류, FDA 기준 미세 오류. 특히 3중 체크에서 한 모델(Grok)만 "논문 알고리즘과 현행 제품 UI가 다르다"를 1차 출처로 입증했고, 반대로 한 모델의 "서지 열람 실패" 주장은 다른 두 모델의 확인으로 기각됐다 — 교차 원칙이 양방향으로 작동한 사례다.
